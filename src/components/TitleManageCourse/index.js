function TitleManageCourse({title}){
    return(
        <div className="main-container-title-manage-course">
            <h2 className="title-manage-course">{title}</h2>
        </div>
    )
}
export default TitleManageCourse;